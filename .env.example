# Home-KRDN 환경 변수 예시
# 이 파일을 복사하여 .env.local 파일을 생성하세요

# Docker
DOCKER_HOST=unix:///var/run/docker.sock

# Authentication (인증)
# JWT 시크릿 키 (32자 이상의 랜덤 문자열 사용)
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# 관리자 계정 설정
ADMIN_USERNAME=admin

# 관리자 비밀번호 해시 (bcrypt)
# 생성 방법: node -e "require('bcryptjs').hash('your-password', 10).then(console.log)"
ADMIN_PASSWORD_HASH=$2a$10$xxxxx...
