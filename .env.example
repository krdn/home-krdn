# Home-KRDN 환경 변수 예시
#
# 환경별 파일 구조:
#   .env                    - 공통 설정 (git 추적)
#   .env.development.local  - 개발 환경 (git 제외)
#   .env.production.local   - 운영 환경 (git 제외)
#   .env.local              - 로컬 오버라이드 (git 제외)
#
# Next.js 우선순위: .env.local > .env.[환경].local > .env.[환경] > .env

# ============================================================
# 공통 설정 (모든 환경)
# ============================================================

# Docker 소켓 경로
DOCKER_HOST=unix:///var/run/docker.sock

# ============================================================
# 개발 환경 (.env.development.local에 복사)
# ============================================================

# Database - 개발용 SQLite
DATABASE_URL="file:./prisma/dev.db"

# JWT Secret - 개발용 (운영에서는 반드시 변경!)
JWT_SECRET=dev-secret-key-do-not-use-in-production

# 로그 레벨
LOG_LEVEL=debug

# ============================================================
# 운영 환경 (.env.production.local에 복사)
# ============================================================

# Database - 운영용 SQLite (별도 경로)
# DATABASE_URL="file:./data/production.db"

# JWT Secret - 운영용 (반드시 새로 생성!)
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# JWT_SECRET=your-production-secret-key

# 로그 레벨
# LOG_LEVEL=info

# ============================================================
# 인증 설정
# ============================================================

# 관리자 계정 (초기 설정용, DB 마이그레이션 후 불필요)
ADMIN_USERNAME=admin
# 비밀번호 해시 생성: node -e "require('bcryptjs').hash('password', 10).then(console.log)"
ADMIN_PASSWORD_HASH=$2a$10$xxxxx...

# ============================================================
# Web Push (VAPID)
# ============================================================

# 키 생성: node scripts/generate-vapid-keys.js
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
NEXT_PUBLIC_VAPID_SUBJECT=mailto:your-email@example.com

# ============================================================
# 외부 서비스 (선택)
# ============================================================

# Resend (이메일)
# RESEND_API_KEY=re_xxxxx

# Slack Webhook
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxxxx
