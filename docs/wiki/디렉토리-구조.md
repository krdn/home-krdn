# 디렉토리 구조

```
/data/home-data/projects/web/home-krdn/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── layout.tsx                # 루트 레이아웃
│   │   ├── page.tsx                  # 랜딩 페이지
│   │   ├── globals.css               # 전역 스타일, CSS 변수
│   │   │
│   │   ├── services/
│   │   │   ├── page.tsx              # 서비스 목록
│   │   │   └── [slug]/
│   │   │       └── page.tsx          # 서비스 상세
│   │   │
│   │   └── admin/
│   │       ├── layout.tsx            # 관리 레이아웃 (사이드바)
│   │       ├── page.tsx              # 대시보드
│   │       ├── services/page.tsx     # 서비스 관리
│   │       ├── containers/page.tsx   # 컨테이너 관리
│   │       ├── logs/page.tsx         # 로그 조회
│   │       └── system/page.tsx       # 시스템 모니터링
│   │
│   ├── components/
│   │   ├── ui/                       # 기본 UI 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Badge.tsx
│   │   │   └── ThemeToggle.tsx
│   │   │
│   │   ├── layout/                   # 레이아웃 컴포넌트
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── MobileSidebar.tsx
│   │   │
│   │   ├── landing/                  # 랜딩 페이지 컴포넌트
│   │   │   ├── Hero.tsx
│   │   │   ├── ServiceCategories.tsx
│   │   │   └── FeaturedServices.tsx
│   │   │
│   │   └── services/                 # 서비스 관련 컴포넌트
│   │       ├── ServiceCard.tsx
│   │       ├── StatusBadge.tsx
│   │       └── TechBadge.tsx
│   │
│   ├── lib/
│   │   ├── utils.ts                  # cn() 등 유틸리티
│   │   └── icons.ts                  # 아이콘 매핑
│   │
│   ├── types/
│   │   ├── service.ts                # 서비스 타입 정의
│   │   └── system.ts                 # 시스템 메트릭 타입
│   │
│   └── config/
│       └── services.ts               # 서비스 설정 데이터
│
├── public/
│   └── images/services/              # 서비스 스크린샷
│
├── docs/
│   └── wiki/                         # Wiki 문서
│
├── deployment/
│   └── nginx/
│       └── nginx.conf                # Nginx 설정
│
├── Dockerfile                        # Docker 빌드
├── docker-compose.yml                # Docker Compose
├── next.config.ts                    # Next.js 설정
├── tailwind.config.ts                # Tailwind 설정
├── tsconfig.json                     # TypeScript 설정
└── package.json                      # 의존성
```

## 주요 파일 설명

### `src/config/services.ts`

모든 서비스 정보를 담고 있는 **단일 진실 소스(Single Source of Truth)**입니다.

```typescript
export const services: Service[] = [
  {
    id: "gonsai2",
    name: "Gonsai2",
    description: "AI 최적화 n8n 워크플로우 관리 플랫폼",
    category: "n8n",
    status: "running",
    techStack: ["Next.js", "Node.js", "MongoDB"],
    port: 8081,
    // ...
  },
  // ... 11개 서비스
];
```

### `src/lib/utils.ts`

Tailwind 클래스 병합을 위한 유틸리티:

```typescript
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

### `src/types/service.ts`

서비스 타입 정의:

```typescript
export interface Service {
  id: string;
  name: string;
  description: string;
  category: ServiceCategory;
  status: ServiceStatus;
  techStack: string[];
  port?: number;
  // ...
}
```
