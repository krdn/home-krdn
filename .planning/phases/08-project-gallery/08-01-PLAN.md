---
phase: 08-project-gallery
plan: 01
type: execute
depends_on: []
files_modified: [src/types/project.ts, src/config/projects.ts, src/app/api/projects/route.ts]
---

<objective>
프로젝트 갤러리의 데이터 모델과 API를 구현합니다.

Purpose: 프로젝트 작업물을 구조화된 형태로 저장하고 조회할 수 있는 기반을 마련합니다.
Output: Project 타입 정의, 프로젝트 설정 파일, API 엔드포인트
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**참고 패턴:**
@src/types/service.ts - 기존 타입 정의 패턴
@src/config/services.ts - 기존 설정 파일 패턴
@src/app/api/system/route.ts - 기존 API 라우트 패턴

**Tech stack available:** Next.js 16, TypeScript, Zod, Tailwind CSS
**Established patterns:** 타입 정의 in src/types/, 설정 in src/config/, API in src/app/api/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로젝트 타입 정의</name>
  <files>src/types/project.ts</files>
  <action>
  프로젝트 갤러리용 타입을 정의합니다:

  ```typescript
  // 프로젝트 카테고리
  export type ProjectCategory = 'web' | 'automation' | 'ai' | 'infra' | 'other';

  // 프로젝트 상태
  export type ProjectStatus = 'active' | 'completed' | 'archived' | 'planned';

  // 기술 스택 항목
  export interface TechStack {
    name: string;
    icon?: string;
    url?: string;
  }

  // 프로젝트 링크
  export interface ProjectLink {
    type: 'github' | 'demo' | 'docs' | 'api' | 'other';
    url: string;
    label?: string;
  }

  // 프로젝트 이미지
  export interface ProjectImage {
    src: string;
    alt: string;
    width?: number;
    height?: number;
    isPrimary?: boolean;
  }

  // 메인 프로젝트 인터페이스
  export interface Project {
    id: string;
    slug: string;
    name: string;
    description: string;
    longDescription?: string;
    category: ProjectCategory;
    status: ProjectStatus;
    techStack: TechStack[];
    links: ProjectLink[];
    images: ProjectImage[];
    features?: string[];
    startDate?: string;
    endDate?: string;
    featured?: boolean;
    order?: number;
  }
  ```

  기존 service.ts 패턴을 따라 타입을 정의합니다.
  </action>
  <verify>npx tsc --noEmit 실행 시 타입 오류 없음</verify>
  <done>Project, ProjectCategory, ProjectStatus 등 모든 타입이 정의됨</done>
</task>

<task type="auto">
  <name>Task 2: 프로젝트 설정 파일 생성</name>
  <files>src/config/projects.ts</files>
  <action>
  src/config/services.ts 패턴을 따라 프로젝트 데이터를 정의합니다:

  - 기본 프로젝트 3-5개 추가 (home-krdn, docker-n8n, ai-note-taking 등)
  - 각 프로젝트에 카테고리, 기술 스택, 링크, 이미지 정보 포함
  - 프로젝트별 features 배열로 주요 기능 설명
  - order 필드로 표시 순서 지정

  실제 프로젝트 정보를 기반으로 데이터를 구성합니다.
  home-krdn 프로젝트는 featured: true로 설정합니다.
  </action>
  <verify>import { projects } from '@/config/projects' 정상 동작</verify>
  <done>최소 3개 이상의 프로젝트 데이터가 정의됨</done>
</task>

<task type="auto">
  <name>Task 3: 프로젝트 API 엔드포인트 생성</name>
  <files>src/app/api/projects/route.ts</files>
  <action>
  프로젝트 목록을 조회하는 API를 구현합니다:

  - GET /api/projects - 전체 프로젝트 목록 반환
  - 쿼리 파라미터: category, status, featured
  - 정렬: order 필드 기준, 없으면 name 기준
  - 기존 system/route.ts 패턴 참고
  - Zod로 쿼리 파라미터 검증

  응답 형식:
  ```json
  {
    "projects": [...],
    "total": number,
    "categories": ["web", "automation", ...]
  }
  ```
  </action>
  <verify>curl http://localhost:3000/api/projects 응답 확인</verify>
  <done>GET /api/projects가 프로젝트 목록을 정상 반환</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` 성공
- [ ] `npx tsc --noEmit` 타입 오류 없음
- [ ] API 응답이 올바른 구조로 반환됨
</verification>

<success_criteria>

- 모든 태스크 완료
- Project 타입 시스템 정의됨
- 프로젝트 데이터 최소 3개 포함
- API 엔드포인트 정상 동작
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-gallery/08-01-SUMMARY.md`
</output>
