---
phase: 15-admin-dashboard
plan: 01
type: execute
depends_on: []
files_modified: [src/components/layout/Sidebar.tsx, src/app/admin/page.tsx, src/components/admin/AdminOverview.tsx]
---

<objective>
Admin Dashboard UI를 통합하여 프로젝트 관리, 알림 설정에 대한 일관된 관리자 경험을 제공합니다.

Purpose: 분산된 Admin 기능들(Projects, Alerts)을 중앙 Dashboard에서 쉽게 접근할 수 있도록 통합
Output: 개선된 Sidebar 네비게이션, 확장된 Quick Access, Admin Overview 요약 카드
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-project-admin-crud/14-01-SUMMARY.md

**기존 파일 참조:**
@src/components/layout/Sidebar.tsx
@src/app/admin/page.tsx
@src/app/admin/alerts/page.tsx
@src/app/admin/projects/page.tsx

**Prior Decisions:**
- Phase 14: JSON 파일 저장 방식, Radix Dialog 인라인 폼
- Phase 7: Zustand persist for alert rules, Radix Toast + Browser Notification

**Tech stack available:**
- Lucide React icons (Bell, FolderKanban, Settings 등)
- Radix UI primitives
- Tailwind CSS

**Established patterns:**
- Sidebar: navigation array + pathname matching
- Dashboard: section-based layout (DashboardStats, ContainerStats, Quick Access)
- Card hover 패턴: `<Card hover className="h-full">`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar 네비게이션 확장 (Alerts 추가 + 그룹화)</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <action>
Sidebar 네비게이션에 Alerts 메뉴를 추가하고 관련 항목들을 논리적으로 그룹화합니다.

1. 네비게이션 배열 수정:
   - 기존 6개 항목 유지 (Dashboard, Projects, Services, Containers, Logs, System)
   - "Alerts" 항목 추가 (Bell 아이콘, href="/admin/alerts")
   - 순서: Dashboard → Projects → Services → Containers → Logs → Alerts → System
   - System은 리소스 모니터링이므로 마지막에 배치

2. lucide-react에서 Bell 아이콘 import 추가

그룹 구분선(divider)은 추가하지 않습니다 - 현재 7개 항목으로 복잡하지 않음.
  </action>
  <verify>
- npm run build 성공
- /admin 페이지에서 Sidebar에 7개 메뉴 표시
- Alerts 클릭 시 /admin/alerts로 이동
  </verify>
  <done>
Sidebar에 Alerts 메뉴가 표시되고 클릭 시 /admin/alerts로 정상 이동
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Quick Access 섹션 확장</name>
  <files>src/app/admin/page.tsx</files>
  <action>
Dashboard의 Quick Access 섹션에 Alerts와 Projects 링크를 추가합니다.

1. Quick Access 섹션을 2x3 그리드에서 2x3 그리드로 확장 (5개 항목):
   - 기존: Services, Containers, System (3개)
   - 추가: Projects (FolderKanban 아이콘), Alerts (Bell 아이콘)
   - 순서: Projects → Alerts → Services → Containers → System

2. 각 카드 구조는 기존 패턴 유지:
   ```tsx
   <Link href="/admin/projects">
     <Card hover className="h-full">
       <CardContent className="flex items-center gap-4 p-6">
         <div className="flex h-12 w-12 items-center justify-center rounded-full bg-primary/10">
           <FolderKanban className="h-6 w-6 text-primary" />
         </div>
         <div>
           <p className="font-medium">Projects</p>
           <p className="text-sm text-muted-foreground">Manage projects</p>
         </div>
       </CardContent>
     </Card>
   </Link>
   ```

3. 그리드 레이아웃:
   - sm: 2열 (최대 5개이므로 마지막 행 3개, 2개로 분할됨)
   - lg: 3열 (최대 5개이므로 2행으로 3+2 배치)
   - 마지막 항목(System)에 `sm:col-span-2 lg:col-span-1` 유지하여 깔끔한 레이아웃

4. lucide-react에서 Bell, FolderKanban 아이콘 import 추가
  </action>
  <verify>
- npm run build 성공
- /admin 페이지에서 Quick Access 섹션에 5개 카드 표시
- 각 카드 클릭 시 해당 페이지로 이동
  </verify>
  <done>
Quick Access 섹션에 Projects, Alerts, Services, Containers, System 5개 카드 표시
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin Overview 카드 컴포넌트 추가</name>
  <files>src/components/admin/AdminOverview.tsx, src/app/admin/page.tsx</files>
  <action>
Dashboard에 Admin 기능 요약을 보여주는 AdminOverview 컴포넌트를 추가합니다.

1. AdminOverview 컴포넌트 생성 (src/components/admin/AdminOverview.tsx):
   - 'use client' 지시문 (API 호출 필요)
   - 카드 형태로 프로젝트 수, 알림 규칙 수 표시
   - API: /api/projects에서 projects 배열 길이
   - Zustand store: useAlertStore에서 rules 배열 길이
   - 레이아웃: 2개 stat을 가로로 배치

2. 컴포넌트 구조:
   ```tsx
   'use client';

   import { useEffect, useState } from 'react';
   import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/Card';
   import { FolderKanban, Bell } from 'lucide-react';
   import { useAlertStore } from '@/stores/alertStore';

   export function AdminOverview() {
     const [projectCount, setProjectCount] = useState(0);
     const rules = useAlertStore((state) => state.rules);

     useEffect(() => {
       fetch('/api/projects')
         .then(res => res.json())
         .then(data => {
           if (data.success) setProjectCount(data.projects.length);
         })
         .catch(() => {});
     }, []);

     return (
       <Card>
         <CardHeader>
           <CardTitle>Admin Overview</CardTitle>
         </CardHeader>
         <CardContent>
           <div className="grid grid-cols-2 gap-4">
             <div className="flex items-center gap-3">
               <FolderKanban className="h-5 w-5 text-muted-foreground" />
               <div>
                 <p className="text-2xl font-bold">{projectCount}</p>
                 <p className="text-sm text-muted-foreground">Projects</p>
               </div>
             </div>
             <div className="flex items-center gap-3">
               <Bell className="h-5 w-5 text-muted-foreground" />
               <div>
                 <p className="text-2xl font-bold">{rules.length}</p>
                 <p className="text-sm text-muted-foreground">Alert Rules</p>
               </div>
             </div>
           </div>
         </CardContent>
       </Card>
     );
   }
   ```

3. Dashboard 페이지에 통합:
   - Section 3 (Containers + Running Services) 아래에 새 섹션 추가
   - AdminOverview를 단독 전체 너비로 배치
   - LazyMetricsCharts처럼 lazy loading은 불필요 (경량 컴포넌트)

4. Dashboard에 import 추가:
   ```tsx
   import { AdminOverview } from '@/components/admin/AdminOverview';
   ```
  </action>
  <verify>
- npm run build 성공
- npm run lint 통과
- /admin 페이지에서 AdminOverview 카드 표시
- 프로젝트 수와 알림 규칙 수가 정확히 표시됨
  </verify>
  <done>
AdminOverview 카드가 Dashboard에 표시되고 프로젝트 수, 알림 규칙 수가 동적으로 로드됨
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] npm run build 성공
- [ ] npm run lint 통과
- [ ] Sidebar에 7개 메뉴 표시 (Dashboard, Projects, Services, Containers, Logs, Alerts, System)
- [ ] Quick Access에 5개 카드 표시
- [ ] AdminOverview 카드에 프로젝트 수, 알림 규칙 수 표시
- [ ] 모든 링크가 올바른 페이지로 이동
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Admin 기능들이 Dashboard에서 쉽게 접근 가능
- Phase 15 complete
</success_criteria>

<output>
After completion, create `.planning/phases/15-admin-dashboard/15-01-SUMMARY.md`:

# Phase 15 Plan 01: Admin Dashboard Summary

**[Substantive one-liner about what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 15 complete, ready for Phase 16 (E2E Testing)
</output>
