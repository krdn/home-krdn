---
phase: 04-ui-ux-enhancement
plan: 01
type: execute
depends_on: []
files_modified: [src/app/globals.css, src/config/design.ts, src/components/ui/Skeleton.tsx]
---

<objective>
디자인 시스템의 기반을 확립합니다: 확장된 디자인 토큰, 타이포그래피 스케일, 애니메이션 프리셋.

Purpose: 일관된 UI/UX를 위한 디자인 언어 정립. 이후 모든 UI 작업의 기반이 됩니다.
Output: 확장된 CSS 변수, 디자인 토큰 설정 파일, 기본 Skeleton 컴포넌트
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md

**현재 상태:**
- 기본 shadcn 스타일 CSS 변수 존재 (background, foreground, primary 등)
- success, warning 색상 정의됨
- 다크/라이트 테마 지원
- 애니메이션 토큰 없음, 타이포그래피 스케일 없음

**확장 목표:**
- 시맨틱 컬러 확장 (info, surface, overlay)
- 타이포그래피 스케일 (heading, body, caption)
- 애니메이션/트랜지션 프리셋
- 스페이싱 스케일
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS 변수 확장 및 디자인 토큰 추가</name>
  <files>src/app/globals.css</files>
  <action>
globals.css에 다음 CSS 변수 추가:

1. **시맨틱 컬러 확장:**
   ```css
   --info: 221 83% 53%;           /* Blue for info states */
   --info-foreground: 0 0% 100%;
   --surface: 240 5% 96%;         /* Elevated surface */
   --surface-foreground: 240 10% 10%;
   ```

2. **타이포그래피 스케일:**
   ```css
   --font-heading: 'Inter', system-ui, sans-serif;
   --font-body: 'Inter', system-ui, sans-serif;
   --text-xs: 0.75rem;
   --text-sm: 0.875rem;
   --text-base: 1rem;
   --text-lg: 1.125rem;
   --text-xl: 1.25rem;
   --text-2xl: 1.5rem;
   --text-3xl: 1.875rem;
   ```

3. **애니메이션 토큰:**
   ```css
   --duration-fast: 150ms;
   --duration-normal: 250ms;
   --duration-slow: 350ms;
   --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
   --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
   ```

4. **스페이싱 스케일 (Tailwind 호환):**
   ```css
   --space-1: 0.25rem;
   --space-2: 0.5rem;
   --space-3: 0.75rem;
   --space-4: 1rem;
   --space-6: 1.5rem;
   --space-8: 2rem;
   ```

5. **Keyframe 애니메이션:**
   ```css
   @keyframes shimmer {
     0% { background-position: -200% 0; }
     100% { background-position: 200% 0; }
   }

   @keyframes fade-in {
     from { opacity: 0; }
     to { opacity: 1; }
   }

   @keyframes slide-up {
     from { opacity: 0; transform: translateY(10px); }
     to { opacity: 1; transform: translateY(0); }
   }
   ```

다크/라이트 테마 모두에 적절한 값 적용. 기존 변수는 유지.
  </action>
  <verify>브라우저 DevTools에서 CSS 변수 확인 가능, `npm run build` 성공</verify>
  <done>CSS 변수 확장 완료, 다크/라이트 테마 모두 적용</done>
</task>

<task type="auto">
  <name>Task 2: 디자인 토큰 설정 파일 생성</name>
  <files>src/config/design.ts</files>
  <action>
TypeScript에서 사용할 디자인 토큰 상수 파일 생성:

```typescript
/**
 * Design System Tokens
 * CSS 변수와 동기화된 디자인 토큰
 */

export const typography = {
  fontFamily: {
    heading: 'var(--font-heading)',
    body: 'var(--font-body)',
  },
  fontSize: {
    xs: 'var(--text-xs)',
    sm: 'var(--text-sm)',
    base: 'var(--text-base)',
    lg: 'var(--text-lg)',
    xl: 'var(--text-xl)',
    '2xl': 'var(--text-2xl)',
    '3xl': 'var(--text-3xl)',
  },
} as const;

export const animation = {
  duration: {
    fast: 'var(--duration-fast)',
    normal: 'var(--duration-normal)',
    slow: 'var(--duration-slow)',
  },
  easing: {
    out: 'var(--ease-out)',
    inOut: 'var(--ease-in-out)',
  },
} as const;

export const spacing = {
  1: 'var(--space-1)',
  2: 'var(--space-2)',
  3: 'var(--space-3)',
  4: 'var(--space-4)',
  6: 'var(--space-6)',
  8: 'var(--space-8)',
} as const;
```

이 파일은 컴포넌트에서 inline style이나 동적 스타일링에 사용됨.
  </action>
  <verify>`npm run build` 시 타입 에러 없음</verify>
  <done>디자인 토큰 설정 파일 생성 완료</done>
</task>

<task type="auto">
  <name>Task 3: Skeleton 컴포넌트 생성</name>
  <files>src/components/ui/Skeleton.tsx</files>
  <action>
로딩 상태를 위한 Skeleton 컴포넌트 생성 (shimmer 애니메이션 활용):

```typescript
import { cn } from "@/lib/utils";

interface SkeletonProps extends React.HTMLAttributes<HTMLDivElement> {
  /** 원형 스켈레톤 (아바타용) */
  circle?: boolean;
}

export function Skeleton({ className, circle, ...props }: SkeletonProps) {
  return (
    <div
      className={cn(
        "animate-pulse bg-muted",
        circle ? "rounded-full" : "rounded-md",
        className
      )}
      {...props}
    />
  );
}

/** 텍스트 라인 스켈레톤 */
export function SkeletonText({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) {
  return (
    <div className={cn("space-y-2", className)} {...props}>
      <Skeleton className="h-4 w-3/4" />
      <Skeleton className="h-4 w-1/2" />
    </div>
  );
}

/** 카드 스켈레톤 */
export function SkeletonCard({ className, ...props }: React.HTMLAttributes<HTMLDivElement>) {
  return (
    <div
      className={cn("rounded-lg border bg-card p-4 space-y-3", className)}
      {...props}
    >
      <Skeleton className="h-4 w-1/3" />
      <Skeleton className="h-20 w-full" />
      <div className="flex gap-2">
        <Skeleton className="h-8 w-16" />
        <Skeleton className="h-8 w-16" />
      </div>
    </div>
  );
}
```

shimmer 애니메이션은 globals.css에 정의된 keyframe 사용.
Tailwind의 animate-pulse 활용 (shimmer는 고급 옵션으로 남겨둠).
  </action>
  <verify>타입 체크 통과, 컴포넌트 import 가능</verify>
  <done>Skeleton, SkeletonText, SkeletonCard 컴포넌트 생성 완료</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` 성공
- [ ] `npm run lint` 에러 없음
- [ ] CSS 변수가 다크/라이트 테마 모두에 정의됨
- [ ] Skeleton 컴포넌트 import 가능
</verification>

<success_criteria>

- 확장된 CSS 변수 (시맨틱 컬러, 타이포그래피, 애니메이션, 스페이싱)
- 디자인 토큰 TypeScript 파일
- Skeleton 컴포넌트 세트
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-ux-enhancement/04-01-SUMMARY.md`
</output>
