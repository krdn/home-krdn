---
phase: 04-ui-ux-enhancement
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [src/app/admin/page.tsx, src/components/admin/DashboardStats.tsx, src/components/admin/ContainerStats.tsx]
---

<objective>
대시보드 레이아웃을 개선하여 정보 계층구조를 명확히 하고 시각적 일관성을 높입니다.

Purpose: 관리자가 시스템 상태를 한눈에 파악할 수 있도록 대시보드 UX 개선
Output: 개선된 대시보드 페이지, 리팩토링된 Stats 컴포넌트
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-ux-enhancement/04-01-SUMMARY.md

**현재 대시보드 구조:**
- DashboardStats: 시스템 메트릭 (CPU, Memory, Disk, Uptime)
- ContainerStats: 컨테이너 상태 카운트
- Running Services 목록
- Quick Links 그리드

**개선 방향:**
- 정보 계층 재구성 (중요도 순)
- 카드 간 시각적 그룹핑
- 로딩 상태 추가 (Skeleton 사용)
- 반응형 그리드 개선
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardStats 컴포넌트 개선</name>
  <files>src/components/admin/DashboardStats.tsx</files>
  <action>
DashboardStats 컴포넌트를 개선:

1. **로딩 상태 추가:**
   - Skeleton 컴포넌트 import하여 로딩 시 표시
   - `isLoading` 상태에 따라 Skeleton/실제 데이터 렌더링

2. **그리드 레이아웃 개선:**
   ```tsx
   <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
   ```

3. **카드 내 정보 계층:**
   - 메인 수치: 큰 폰트, 볼드
   - 레이블: 작은 폰트, muted
   - 아이콘: 배경 원형으로 강조

4. **값 포맷팅 개선:**
   - CPU: 소수점 1자리 + % 표시
   - Memory: formatBytes 활용
   - 진행률 바 추가 (CPU, Memory, Disk)

예시 카드 구조:
```tsx
<Card>
  <CardContent className="p-6">
    <div className="flex items-center justify-between">
      <div>
        <p className="text-sm font-medium text-muted-foreground">CPU</p>
        <p className="text-2xl font-bold">{cpu.toFixed(1)}%</p>
      </div>
      <div className="flex h-12 w-12 items-center justify-center rounded-full bg-primary/10">
        <Cpu className="h-6 w-6 text-primary" />
      </div>
    </div>
    {/* Progress bar */}
    <div className="mt-4 h-2 w-full rounded-full bg-secondary">
      <div
        className="h-2 rounded-full bg-primary transition-all"
        style={{ width: `${Math.min(cpu, 100)}%` }}
      />
    </div>
  </CardContent>
</Card>
```
  </action>
  <verify>대시보드 페이지에서 로딩 스켈레톤 표시 후 데이터 렌더링 확인</verify>
  <done>DashboardStats에 로딩 상태, 진행률 바, 개선된 레이아웃 적용</done>
</task>

<task type="auto">
  <name>Task 2: ContainerStats 컴포넌트 개선</name>
  <files>src/components/admin/ContainerStats.tsx</files>
  <action>
ContainerStats 컴포넌트 개선:

1. **로딩 상태 추가:**
   - Skeleton 컴포넌트로 로딩 표시

2. **시각적 개선:**
   - 상태별 색상 강조 (running: success, stopped: muted, error: destructive)
   - 아이콘과 숫자 조합으로 명확한 표시

3. **레이아웃:**
   ```tsx
   <Card>
     <CardHeader>
       <CardTitle className="flex items-center gap-2">
         <Box className="h-5 w-5" />
         Containers
       </CardTitle>
     </CardHeader>
     <CardContent>
       <div className="grid grid-cols-3 gap-4 text-center">
         <div>
           <p className="text-2xl font-bold text-success">{running}</p>
           <p className="text-sm text-muted-foreground">Running</p>
         </div>
         <div>
           <p className="text-2xl font-bold text-muted-foreground">{stopped}</p>
           <p className="text-sm text-muted-foreground">Stopped</p>
         </div>
         <div>
           <p className="text-2xl font-bold">{total}</p>
           <p className="text-sm text-muted-foreground">Total</p>
         </div>
       </div>
     </CardContent>
   </Card>
   ```

4. **상태 변화 시 시각적 피드백:**
   - 숫자 변경 시 subtle 트랜지션
  </action>
  <verify>컨테이너 상태가 색상으로 구분되어 표시됨</verify>
  <done>ContainerStats 시각적 개선 및 로딩 상태 추가</done>
</task>

<task type="auto">
  <name>Task 3: 대시보드 페이지 레이아웃 재구성</name>
  <files>src/app/admin/page.tsx</files>
  <action>
대시보드 페이지 레이아웃 개선:

1. **정보 계층 재구성:**
   - 1행: System Stats (4열 그리드)
   - 2행: Container Stats + Running Services (2열)
   - 3행: Quick Links (3열)

2. **섹션 구분:**
   ```tsx
   <div className="space-y-8">
     {/* Header */}
     <header>
       <h1 className="text-3xl font-bold tracking-tight">Dashboard</h1>
       <p className="text-muted-foreground">시스템 상태 및 서비스 개요</p>
     </header>

     {/* System Metrics - Primary */}
     <section>
       <DashboardStats />
     </section>

     {/* Containers + Services - Secondary */}
     <section className="grid gap-6 lg:grid-cols-2">
       <ContainerStats />
       <RunningServicesCard />
     </section>

     {/* Quick Links - Tertiary */}
     <section>
       <h2 className="mb-4 text-lg font-semibold">Quick Access</h2>
       <div className="grid gap-4 sm:grid-cols-3">
         {/* Quick link cards */}
       </div>
     </section>
   </div>
   ```

3. **반응형 개선:**
   - 모바일: 1열 스택
   - 태블릿: 2열 그리드
   - 데스크톱: 지정된 레이아웃

4. **Running Services를 별도 카드 컴포넌트로 추출** (선택적, 시간 허용 시)
  </action>
  <verify>`npm run dev`로 대시보드 렌더링 확인, 반응형 동작 확인</verify>
  <done>대시보드 레이아웃 재구성 완료, 정보 계층 명확화</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` 성공
- [ ] `npm run lint` 에러 없음
- [ ] 대시보드 로딩 시 Skeleton 표시
- [ ] 반응형 레이아웃 동작 (모바일/태블릿/데스크톱)
</verification>

<success_criteria>

- DashboardStats에 로딩 상태와 진행률 바 추가
- ContainerStats 시각적 개선
- 대시보드 레이아웃 재구성
- 모든 빌드/린트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-ux-enhancement/04-02-SUMMARY.md`
</output>
